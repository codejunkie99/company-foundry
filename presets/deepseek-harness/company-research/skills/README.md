# Company Research Skills

This preset is self-contained. Its scoped registry includes `company-work-packet`, `customer-evidence-research`, `model-route-record`, `company-dashboard`, and `company-artifact-review`.

The portable source copies live in the repository root under `skills/`. Keep both copies aligned when changing a method.
