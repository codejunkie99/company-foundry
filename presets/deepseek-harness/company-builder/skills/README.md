# Company Builder Skills

This preset is self-contained. Its scoped registry includes `company-work-packet`, `model-route-record`, `company-code-delivery`, `company-ui-artifact`, `company-dashboard`, and `company-artifact-review`.

The portable source copies live in the repository root under `skills/`. Keep both copies aligned when changing a method.
